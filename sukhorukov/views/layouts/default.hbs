<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>
		{{#if parsedData}}
			OS TOP{{parsedData.topOf}}
		{{else}}
			404
		{{/if}}
	</title>
	<link rel="stylesheet" href="/main.css">
</head>
<body>
	<div class="container">
		{{#if parsedData}}
			{{> header}}
		{{/if}}
		{{{body}}}
	</div>
</body>
</html>